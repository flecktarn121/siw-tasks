# Consultas sobre wikidata

La siguiente consulta son todas las combianciones de propiedad-objeto realcionados con el Greco en todos los idiomas.

```sparql
SELECT distinct ?p ?o {
    wd:Q301 ?p ?o
}
```

```sparql
SELECT distinct ?p ?o {
    wd:Q301 ?p ?o
}
```
